import { PageLoader } from "@/components/layout/loader";

export default function Loading() {
  return <PageLoader label="Loading your portal" description="Fetching your quotations and invoices." />;
}
